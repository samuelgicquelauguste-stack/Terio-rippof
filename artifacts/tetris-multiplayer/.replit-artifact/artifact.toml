kind = "web"
previewPath = "/"
title = "Multiplayer Tetris"
version = "1.0.0"
id = "artifacts/tetris-multiplayer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25223

[services.development]
run = "pnpm --filter @workspace/tetris-multiplayer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tetris-multiplayer", "run", "build" ]
publicDir = "artifacts/tetris-multiplayer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25223"
BASE_PATH = "/"
